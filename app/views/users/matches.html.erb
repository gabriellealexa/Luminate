<h1>My Matches</h1>

<%= @users.each do |user| %><br>

<%= link_to "View #{user.first_name}", user_path(user) %>

          <%= button_to "Remove", unlike_path(user.id), method: :delete, :class => "btn btn-default", :id => "like_button" %>


  <% end %>

